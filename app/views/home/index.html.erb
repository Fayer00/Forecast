<% if user_signed_in? %>
  <div> Welcome <%= current_user.email %> </div>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete, id: 'sign-out-link' %>
<% else %>
  <%= button_to "Sign in", new_user_session_path, id: 'sign-in-link' %>
<% end %>
<h1>Hello DigitalOcean!</h1>

<%= link_to 'Check Weather', weather_path %>>